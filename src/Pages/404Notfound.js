import * as React from "react";

const NotFound = () => {
  return (
    <div
      style={{
        textAlign: "center",
        fontSize: "xx-large",
        height: "90vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <div>Not Found!!</div>
    </div>
  );
};

export default NotFound;
